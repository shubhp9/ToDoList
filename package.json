{
  "name": "todolist",
  "version": "1.0.0",
  "description": "To-do List App",
  "main": "index.js",
  "scripts": {
    "start": "electron .",
    "package-win": "electron-packager . to-do-list-app --overwrite --asar=true --platform=win32 --arch=ia32 --icon=assets/icons/win/icon.ico --prune=true --out=release-builds --version-string.CompanyName=CE --version-string.FileDescription=CE --version-string.ProductName=\"To-do List\""   
    },
  "author": "Shubh Patel",
  "license": "ISC",
  "dependencies": {
    "electron": "^9.1.2"
  },
  "devDependencies": {
    "electron-packager": "^15.0.0"
  }
}
